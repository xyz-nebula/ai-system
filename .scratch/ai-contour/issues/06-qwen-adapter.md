# 06: Живой текстовый ход через Qwen

**What to build:** AI Engineer подключает доступный текстовый Qwen endpoint через конфигурацию сервиса и проводит тот же кейс в терминальном клиенте с ответами реальной модели. Управляемая модель остаётся средством автоматических тестов.

**Blocked by:** 02: Защищённый ход.

Status: ready-for-agent

- [x] После получения несекретных параметров Model/Infra сервис использует указанный endpoint и модель без изменения предметной логики хода.
- [x] Терминальный клиент проходит несколько текстовых ходов с живым Qwen, сохраняя Guard и проверку ответа до принятия реплики.
- [x] Таймаут, недоступность endpoint и неверный структурированный вывод дают явную безопасную ошибку без раскрытия сырого ответа или секретов.
- [x] Семантическая проверка на Qwen дополняет локальные правила Guard и валидатора: перефразированные закрытые позиции и противоречивое согласие не становятся принятым ходом; при неопределённом результате проверка завершается безопасной ошибкой.
- [x] Автоматические проверки адаптера используют управляемые ответы; секреты передаются только через локальную конфигурацию и не попадают в логи или документацию.

## Comments

- 2026-09-20: Подготовлен настраиваемый OpenAI-совместимый транспорт, изолированные роли Qwen и семантические швы Guard/Validator. Сквозной API-тест на управляемом ответе проходит семь отдельных вызовов через один endpoint. Реальный формат, параметры reasoning/JSON и ручной прогон ожидают несекретных сведений Model/Infra; до этого критерии живого endpoint и качества остаются открытыми.
- 2026-09-20: `/code-review` от `afe3c87`: Standards — 0 нарушений, 2 замечания о дублировании очистки транскрипта и смешении runtime-сборки с HTTP-обработчиками; Spec — 1 ошибка: 30-секундный таймаут CLI меньше суммарной длительности модельных вызовов. Очистка теперь общая, Qwen-сборка вынесена в `runtime.py`, таймауты хода и завершения настраиваются отдельно. Живой прогон остаётся открытым.
- 2026-09-22: Проверен живой Model/Infra gateway с `qwen3.8-9b-q4` через локальный SSH-туннель. Gateway возвращает структурированный результат как чистый JSON или цельный Markdown-блок `json`; режим `json_object` не возвращает итог в ожидаемом `message.content`, поэтому рабочая конфигурация использует `prompt`. Добавлен строгий разбор одного fenced JSON с регрессионным тестом через публичный `POST /v1/turn`.
- 2026-09-22: Терминальный клиент прошёл два последовательных живых хода до допустимого соглашения. После уточнения инструкции Guard перефразированный запрос неназванной уступки заблокирован до вызова оппонента; явное несогласие не стало соглашением и завершилось безопасным `invalid_opponent_output`. Два транзиентных upstream-отказа завершились `guard_unavailable` без изменения состояния. Полная приёмка судей и тренера остаётся в тикете 07.
- 2026-09-22: `/code-review` незакоммиченного diff: Standards — без жёстких нарушений, исправлены устаревший README и мелкое дублирование test helper; низкоуверенное замечание о совместном изменении parser и Guard принято как один результат live-приёмки. Spec — реализация соответствует тикету, после review checklist и evidence зафиксированы в этом комментарии.
